from googlesearch import search
from core.formatting import *
from core.utilities import *

class Dorker:

	def __init__(self, search, start):
		self.search = search
		self.start = start






'''
def gsearch(query, start):
	return [i for i in search(query,start=start, num=10, pause=2)]

def save_increments(start):
	with open('core/dorking/starts.txt','r') as f:
		f.write(start)
		f.close()

'''